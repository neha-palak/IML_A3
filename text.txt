to write down our thoughts maybe
not needed for the assignment

what the hellyyyy

done till text preprocessing
image preprocessing left

try to subsample using a fixed percentage



tokenize emotions
token_c1 already exists as tokenization of utter_c1
concatenate token_emotions + token_c1

mention which one is best in vlt_history.json



# ✅ **LIST OF ALL CORRECTIONS YOU MUST MAKE**

### **Model Architecture**
* increase CNN layer by one
* Do NOT feed image features at every timestep.
* Use image features only to initialize the LSTM hidden state.
* Remove TF-IDF-based embeddings from the decoder.
* Use proper learned word embeddings (or GloVe/FastText).
* Fix LSTM input shape (token embedding only).
* Fix hidden state handling: keep `(h, c)` across decoding steps.

### **Training**

* Train for more epochs (20–40).
* Remove `<pad>`, `<start>`, `<end>` from prediction targets.
* Apply loss masking so padded tokens do not contribute to loss.
* Ensure image features are correctly precomputed and loaded.
* Ensure emotions are encoded correctly and consistently.
* Ensure teacher forcing is correctly implemented.

### **Decoding (Greedy)**

* Decode **one step at a time**, not the entire sequence each loop.
* At each step, feed **only the last predicted token**, not the full history.
* Preserve LSTM hidden state between steps.
* Stop when `<end>` token is predicted.
* Do not repetitively recompute the entire LSTM from scratch.

### **Dataset + Preprocessing**

* Remove special tokens from reference captions before evaluation.
* Ensure padding happens after `<end>` token positions only.
* Validate that `features/{painting}.npy` contains extracted CNN features, not raw images.
* Ensure text vocabulary excludes `<pad>` from training loss.
* Ensure consistent max sequence length between train and eval.

### **Evaluation**

* Clean both hypothesis and reference captions (remove special tokens).
* Ensure both are tokenized consistently before BLEU/ROUGE.
* Check maximum sample count logic.


Hi students,
For Assignment 3, I got few concerns that students have misunderstood the inputs of the model. As a matter of clarity, there are two inputs - image (WikiArt dataset) and emotion (Artemis dataset) and Output - caption (Artemis dataset).

As the deadline is very close, if you have already trained the model without the emotions, please mention such caveats at the presentation. In case you haven't trained the models yet, its suggested to use the emotions for getting better captions.

Hi,
Please prepare a 10 minutes presentation for the Assignment 3.
The presentation should include all the efforts you did, architecture, results, and analysis. (Note: You can have as many number slides as per your convenience taking into account the time limit)
In the next 10 mins, we will share few images and emotions, your task will be to execute it and show the generated captions from the two models. You can create a predict.py file that takes these inputs, do the pre-processing and generate the results from the trained models.



5. Model 1 – CNN + LSTM Network 
Architecture 
●  Use a custom CNN (that is not pretrained) as the image encoder to extract image_features with multiple convolutional and pooling layers.  
●  Output a compact image feature vector (256D).  
●  Build an LSTM or BiLSTM network to generate captions word by word (sequence to sequence approach) 
●  Inputs: emotion embedding+ caption word embedding + image_feature vector.  
●  Outputs: Next word in the sequence. 


using the combined vector, to initialize the lstm's hidden and cell state 
fusing the image feature, emotion vector and the word embedding at each step as input to the lstm 
allowing lstm to focus on image regions or emotional features as it generates each word of the caption


●  tell me how too Experiment with:   
○  Hidden sizes (e.g., 128, 256) 
○  Dropout layers  
Training Objective 
●  Use Categorical Cross-Entropy Loss (mask padding tokens).  
●  Optimize using Adam optimizers.  
●  Track training and validation loss per epoch and save it as a summary json 

Evaluation 
●  Generate captions for test images, with different emotions  
●  Compare generated vs. ground truth captions using metrics like BLEU, ROUGE, and CIDEr.  
●  Display a few sample images with their predicted captions, ground truth and emotion.   


Experiment:
hidden layer 128,256,512: larger sizes capture more complex dependencies but increase training time and risk overfitting 
dropout layer : lstm dropout to the output before the linear layer is 0.3-0.5 
 